function findTags() {
    // Retrieve the <p> element as HTMLElement object
    const array = document.getElementsByTagName("div").innerHTML = "Hello";

    // Update the 'innerHTML' property
    p_element_object.innerHTML = "Alex De";
}
